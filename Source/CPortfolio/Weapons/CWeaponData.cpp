#include "CWeaponData.h"
#include "Global.h"

void UCWeaponData::EndPlay(ACharacter* InOwner)
{
/* 	if (!!Attachment)
	{
		Attachment->Destroy();
		Attachment = nullptr;
	}

	if (!!Equipment)
	{
		Equipment->ConditionalBeginDestroy();
		Equipment = nullptr;
	}

	if (!!Attack)
	{
		Attack->ConditionalBeginDestroy();
		Attack = nullptr;
	}

	if (!!SubAction)
	{
		SubAction->ConditionalBeginDestroy();
		SubAction = nullptr;
	} */
}

